extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 Searching for: #{searchBy}
    .search__videos
        each item in videos
            +videoBlock({
                title: item.title,
                videoFile: item.videoFile,
                views: item.views
            })