extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        each item in videos
            +videoBlock({
                id: item.id,
                title: item.title,
                videoFile: item.fileUrl,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                title: item.title,
                videoFile: item.fileUrl,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                title: item.title,
                videoFile: item.fileUrl,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                title: item.title,
                videoFile: item.fileUrl,
                views: item.views
            })